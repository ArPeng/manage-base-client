export default {
  sign: {
    // 登录
    signByPassword: '/sign.by_password',
    // 验证Token
    verificationToken: '/sign.verification_token',
    // 退出时清除token
    clearToken: '/sign.clear_token'
  }
}
